const express = require("express");
const app = express();
let {people} = require('./data')


app.get('/api/people', )







app.listen(8000, () => {
  console.log("Server is listening on port 8000....");
});
